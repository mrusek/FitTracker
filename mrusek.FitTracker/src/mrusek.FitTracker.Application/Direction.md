Logika przypadków użycia — bez świadomości szczegółów technicznych.

Zawiera:

Use Cases (Features) – np. CreateUserCommand, GetUserQuery

Behaviors / Pipelines – np. walidacja, logowanie, transakcje

Interfaces – np. IUserRepository, IEmailService

DTOs – obiekty transportowe między warstwami